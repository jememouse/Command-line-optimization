# 🚀 现代化命令行工具指南

这份指南为你介绍那些能让终端体验焕然一新的现代化工具。它们大多使用 Rust 或 Go 编写，主打**高性能**、**高颜值**和**人性化交互**。

## 📂 导航与文件查看 (Navigation & Viewing)

### 1. [zoxide](https://github.com/ajeetdsouza/zoxide) - 更智能的 `cd`

* **代替**: `cd`
* **痛点**: 每次进深层目录都要打 `cd ~/code/project/backend/src/...`，很累。
* **解决方案**: `zoxide` 会记住你经常访问的目录。
* **用法**:
  * `z src` -> 自动跳转到最近/最常使用的带 `src` 的目录。
  * `zi` -> 交互式选择目录。

### 2. [eza](https://github.com/eza-community/eza) - 现代化的 `ls`

* **代替**: `ls`
* **痛点**: `ls` 输出单调，很难一眼区分文件类型、权限和 Git 状态。
* **解决方案**: `eza` 提供图标支持、漂亮的配色、Git 状态集成和树状视图。
* **用法**:
  * `eza` -> 列出文件。
  * `eza -l --icons --git` -> 详细列表，带图标和 Git 状态。
  * `eza --tree --level=2` -> 显示两层目录树。

### 3. [bat](https://github.com/sharkdp/bat) - 给了翅膀的 `cat`

* **代替**: `cat`
* **痛点**: `cat` 查看代码文件时没有语法高亮，也没有行号，看长文件很痛苦。
* **解决方案**: `bat` 支持几乎所有语言的语法高亮，显示行号，且自动分页。
* **用法**:
  * `bat main.py` -> 高亮显示 Python 代码。
  * `bat --diff README.md` -> 甚至能标记出文件相对于 Git 的修改行。

### 4. [fzf](https://github.com/junegunn/fzf) - 命令行模糊搜索神器

* **代替**: `Ctrl+R` (历史搜索) / `find`
* **痛点**: 按向上箭头找很久以前的命令；或者想对一堆文件做操作但文件名记不清。
* **解决方案**: 通用的模糊搜索器，可以和任何命令组合。
* **用法**:
  * `Ctrl+R` -> 呼出交互式历史搜索，支持模糊匹配。
  * `history | fzf` -> 在历史中搜索。
  * `vim $(fzf)` -> 搜索文件并用 vim 打开。

---

## 🔍 搜索大师 (Search)

### 5. [ripgrep (rg)](https://github.com/BurntSushi/ripgrep) - 光速内容搜索

* **代替**: `grep`
* **痛点**: `grep` 慢，且默认搜索 `.git`, `node_modules` 等垃圾目录。
* **解决方案**: 极快，自动忽略 `.gitignore` 中的文件。
* **用法**:
  * `rg "TODO"` -> 搜索当前目录下所有包含 "TODO" 的文件。
  * `rg "def main" -t py` -> 只在 Python 文件中搜索。

### 6. [fd](https://github.com/sharkdp/fd) - 简单好用的文件查找

* **代替**: `find`
* **痛点**: `find` 语法反人类 (`find . -name "*.py"`?)，且慢。
* **解决方案**: 语法简单，速度快，彩色输出。
* **用法**:
  * `fd pattern` -> 查找包含 pattern 的文件。
  * `fd -e jpg` -> 查找所有 jpg 图片。

---

## 🛠 开发与运维 (Dev & Ops)

### 7. [lazygit](https://github.com/jesseduffield/lazygit) - 终端里的 Git 图形界面

* **代替**: 各种 Git GUI 客户端 或 复杂的 Git 命令
* **痛点**: 命令行解决 Git 冲突很麻烦；只想暂存（Stage）文件的一部分。
* **解决方案**: 发呆式 Git 操作。全键盘控制，极其直观。
* **用法**:
  * `lazygit` -> 打开界面。空格键暂存，`c` 提交，`P` 推送。

### 8. [delta](https://github.com/dandavison/delta) - 能看懂的 Git Diff

* **代替**: 默认的 `git diff` 输出
* **痛点**: 默认 diff 是一堆红绿文字，难以看清行内具体的单词变化。
* **解决方案**: 语法高亮，行内单词级差异对比，Side-by-side 视图。
* **用法**: 配置后，运行 `git diff` 自动生效。

### 9. [btop](https://github.com/aristocratos/btop) - 赛博朋克系统监控

* **代替**: `top`, `htop`
* **痛点**: `top` 丑且信息难读。
* **解决方案**: 界面极其酷炫，支持鼠标交互，清晰展示 CPU、内存、网络、硬盘和进程树。
* **用法**:
  * `btop` -> 启动监控中心。

---

## 💡 实用小帮手 (Utilities)

### 10. [tldr](https://github.com/tldr-pages/tldr) - 太长不看 (Too Long; Didn't Read)

* **代替**: `man`
* **痛点**: `man tar` 出来几千行文档，你只想知道怎么解压。
* **解决方案**: 社区维护的命令 Cheatsheet，只列出最常用的例子。
* **用法**:
  * `tldr tar` -> 显示 tar 的常用命令示例。

### 11. [httpie](https://github.com/httpie/httpie) - 人性化 HTTP 客户端

* **代替**: `curl`
* **痛点**: `curl` 参数多，输出不格式化。
* **解决方案**: 像写自然语言一样写请求，自动高亮 JSON。
* **用法**:
  * `http GET google.com`
  * `http POST api.com/users name=John age:=20`
